const { responderPreguntas } = require('./src/funciones/niluChat');
const { verificarRacha } = require('./src/funciones/notificacionRacha');

// Exportar todas las funciones
module.exports = {
  responderPreguntas,
  verificarRacha
};
